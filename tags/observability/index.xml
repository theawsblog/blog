<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Observability | The AWS Blog</title><link>https://theawsblog.com/tags/observability/</link><description>Articles, tutorials and insights from the AWS community.</description><generator>Hugo</generator><language>en</language><managingEditor>@theawsblog (The AWS Blog)</managingEditor><webMaster>@theawsblog</webMaster><lastBuildDate>Fri, 07 Aug 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://theawsblog.com/tags/observability/index.xml" rel="self" type="application/rss+xml"/><item><title>Cross-Account ECS Telemetry Needs a Platform Boundary</title><link>https://theawsblog.com/news/emiliano-montesdeoca/ecs-cross-account-telemetry-adot-gateway/</link><pubDate>Fri, 07 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/ecs-cross-account-telemetry-adot-gateway/</guid><description>A centralized AWS Distro for OpenTelemetry gateway can reduce per-task overhead and close observability gaps across multi-account ECS environments, including Windows .NET workloads.</description><content:encoded>&lt;p&gt;A collector sidecar is a friendly pattern right up until an organization has hundreds of ECS tasks. Then the same convenience becomes a platform tax: every task carries collector CPU and memory, every account carries configuration, and every update becomes another rollout.&lt;/p&gt;
&lt;p&gt;The problem is even sharper for Windows .NET Framework workloads. A Linux collector sidecar is not a universal answer for an IIS application running on Windows. You either create a separate collection strategy or accept a hole in the telemetry map.&lt;/p&gt;
&lt;p&gt;The AWS Containers Blog&amp;rsquo;s &lt;a href="https://aws.amazon.com/blogs/containers/centralize-cross-account-amazon-ecs-telemetry-with-an-adot-gateway/"&gt;cross-account ECS telemetry pattern&lt;/a&gt; moves collection into a dedicated observability account. Workloads send OpenTelemetry Protocol data over private connectivity to a centralized AWS Distro for OpenTelemetry gateway, which exports traces to AWS X-Ray and metrics and logs to CloudWatch.&lt;/p&gt;
&lt;p&gt;That is more than a collector deployment. It is an observability platform boundary.&lt;/p&gt;
&lt;h2 id="sidecar-versus-gateway"&gt;Sidecar versus gateway&lt;/h2&gt;
&lt;p&gt;The sidecar model has a real advantage: the collector sits next to the application and is easy to understand. For a small service, that local ownership may be exactly right. The cost appears when the pattern is copied everywhere.&lt;/p&gt;
&lt;p&gt;A collector per task multiplies baseline resource usage. It also multiplies configuration drift. A change to batching, sampling, authentication, or metric dimensions must reach every task definition and every account. Windows workloads introduce another constraint because the application may not be able to host the same Linux sidecar at all.&lt;/p&gt;
&lt;p&gt;A centralized gateway reverses those trade-offs:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;one fleet to patch and configure,&lt;/li&gt;
&lt;li&gt;one endpoint for workloads across accounts,&lt;/li&gt;
&lt;li&gt;one place to control batching and export behavior,&lt;/li&gt;
&lt;li&gt;a shared path for Linux and Windows applications.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The price is network and platform complexity. You need private routing between workload VPCs and the observability VPC, capacity planning for the gateway, and a clear ownership model for telemetry arriving from many accounts.&lt;/p&gt;
&lt;h2 id="windows-net-is-the-forcing-function"&gt;Windows .NET is the forcing function&lt;/h2&gt;
&lt;p&gt;For a Windows .NET Framework application hosted in IIS, the gateway is not just a cost optimization. It can be the practical way to reach the destination telemetry systems without running a Linux collector beside the application.&lt;/p&gt;
&lt;p&gt;The source pattern configures OpenTelemetry instrumentation in the Windows image and points the OTLP exporter at the gateway&amp;rsquo;s internal network load balancer. IIS worker processes need the right machine-level environment variables and instrumentation registration. That detail is easy to miss because a Linux container tutorial can make environment configuration look universal.&lt;/p&gt;
&lt;p&gt;The useful design principle is to keep application instrumentation consistent while moving platform-specific collection into the shared gateway. The application emits standard OTLP. The gateway owns export, batching, dimensions, and destination-specific details.&lt;/p&gt;
&lt;h2 id="the-failure-mode-that-wastes-a-day"&gt;The failure mode that wastes a day&lt;/h2&gt;
&lt;p&gt;Centralization does not make networking disappear. One common failure is a target health-check loop: ECS tasks send telemetry to the load balancer, but the collector targets never become healthy. The security group allows client CIDRs but blocks health checks originating from the load balancer subnets.&lt;/p&gt;
&lt;p&gt;Allow the health-check path explicitly. Test the gateway&amp;rsquo;s health endpoint from the load balancer perspective, then test OTLP reachability from a workload VPC. A collector can be running and still be unreachable, which makes task logs unnecessarily confusing.&lt;/p&gt;
&lt;p&gt;Preserve source identity as well. The gateway should add fallback metadata only when the workload did not provide it. If the collector overwrites account, cluster, or service attributes, all telemetry starts looking as if it came from the observability account. Centralization is useful only when the origin remains trustworthy.&lt;/p&gt;
&lt;h2 id="choose-the-layer-that-is-missing"&gt;Choose the layer that is missing&lt;/h2&gt;
&lt;p&gt;The AWS article distinguishes collection from visualization. CloudWatch cross-account observability can give operators a shared view of telemetry that is already being collected. It does not solve an instrumentation or ingestion gap. An ADOT gateway operates earlier, at the collection boundary.&lt;/p&gt;
&lt;p&gt;That distinction keeps teams from deploying a gateway to solve the wrong problem. If every workload already emits reliable telemetry and the pain is searching across accounts, use the native cross-account viewing features. If applications cannot collect consistently, or you need one governed exporter configuration, the gateway is the better fit.&lt;/p&gt;
&lt;h2 id="a-practical-rollout"&gt;A practical rollout&lt;/h2&gt;
&lt;p&gt;I would start with one observability account, two Availability Zones, and one representative Linux service plus one Windows .NET service. Before onboarding more accounts:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Establish Transit Gateway or equivalent private routing and verify non-overlapping CIDR ranges.&lt;/li&gt;
&lt;li&gt;Run the gateway behind an internal load balancer and alarm on unhealthy targets.&lt;/li&gt;
&lt;li&gt;Send a test trace, metric, and log from each operating-system family.&lt;/li&gt;
&lt;li&gt;Check that account, cluster, service, and environment dimensions survive the gateway.&lt;/li&gt;
&lt;li&gt;Limit CloudWatch metric dimensions to fields you actually query; high-cardinality metadata becomes an ingestion bill quickly.&lt;/li&gt;
&lt;li&gt;Add a second collector task and exercise a replacement while telemetry is flowing.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The source article estimates that a small centralized gateway can run in the low tens of dollars per month before telemetry ingestion costs. The exact number will vary, but the broader lesson is stable: compare one governed fleet with the cumulative cost of per-task collectors, not with a zero-cost imaginary sidecar.&lt;/p&gt;
&lt;p&gt;A centralized ADOT gateway is not automatically the right answer for every ECS estate. It is a strong answer when multi-account scale, Windows .NET support, and consistent collection are the actual constraints. Treat it as platform infrastructure, preserve source identity, and test the failure path before calling observability complete.&lt;/p&gt;</content:encoded></item><item><title>On-Premises Syslog Becomes Useful Evidence When Hybrid Operations Can Trust It</title><link>https://theawsblog.com/news/emiliano-montesdeoca/devops-agent-onpremises-cloudwatch-visibility/</link><pubDate>Fri, 07 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/devops-agent-onpremises-cloudwatch-visibility/</guid><description>Managed syslog ingestion and CloudWatch Log Alarms can connect on-premises network events to AWS DevOps Agent investigations, but delivery and normalization need to be proven first.</description><content:encoded>&lt;p&gt;When a branch firewall blocks traffic or a VPN tunnel drops, the device usually records the event. That does not make the event useful to an incident responder. A log trapped on an appliance is hard to correlate with AWS telemetry, hard to alert on, and easy to lose when the network path is already failing.&lt;/p&gt;
&lt;p&gt;The AWS Cloud Operations Blog&amp;rsquo;s &lt;a href="https://aws.amazon.com/blogs/mt/use-cloudwatch-syslog-and-log-alarms-to-give-aws-devops-agent-on-premises-visibility/"&gt;managed syslog and Log Alarms pattern&lt;/a&gt; connects on-premises device logs to CloudWatch and AWS DevOps Agent. The practical value is not just removing a collector. It is making hybrid evidence searchable in the same place as application and infrastructure signals.&lt;/p&gt;
&lt;h2 id="the-trust-equation"&gt;The trust equation&lt;/h2&gt;
&lt;p&gt;For an agent to investigate a hybrid incident, four things need to be true:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;messages arrive without silent loss,&lt;/li&gt;
&lt;li&gt;fields such as severity and hostname are parsed consistently,&lt;/li&gt;
&lt;li&gt;retention and access policies make the history available,&lt;/li&gt;
&lt;li&gt;the agent can distinguish device evidence from an incomplete feed.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Miss one and the investigation can become confidently wrong. A missing firewall event may look like a clean network path. An unparsed vendor message may hide the field needed to correlate it with a service outage.&lt;/p&gt;
&lt;p&gt;CloudWatch managed syslog ingestion provides a path from devices through a private VPC endpoint into CloudWatch Logs. The source pattern then uses Log Alarms to run queries and notify the DevOps Agent through a controlled integration.&lt;/p&gt;
&lt;h2 id="private-ingestion-is-only-the-start"&gt;Private ingestion is only the start&lt;/h2&gt;
&lt;p&gt;Use private connectivity and TLS for the transport, but also plan for the transport to fail. Site-to-Site VPN should use both tunnels. Direct Connect should have a documented backup path. Devices should buffer messages locally when possible, and TCP with TLS is generally easier to reason about than UDP when dropped packets matter.&lt;/p&gt;
&lt;p&gt;On the AWS side, restrict the VPC endpoint and log group policies to the intended account and network. The syslog service path may not authenticate like a normal API caller, so condition keys and endpoint scoping deserve careful testing.&lt;/p&gt;
&lt;p&gt;Once messages arrive, validate the fields CloudWatch extracts from the formats your devices actually emit. A lab &lt;code&gt;logger&lt;/code&gt; command proves the endpoint works. It does not prove that a production firewall&amp;rsquo;s vendor-specific format will be parsed correctly.&lt;/p&gt;
&lt;h2 id="log-queries-can-become-operational-signals"&gt;Log queries can become operational signals&lt;/h2&gt;
&lt;p&gt;Log Alarms let a query become a detector without a separate metric-filter pipeline. A query for a known deny pattern or a tunnel state change can transition an alarm and invoke the agent through SNS and a small Lambda integration.&lt;/p&gt;
&lt;p&gt;That creates a useful flow:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;The device emits an event.&lt;/li&gt;
&lt;li&gt;CloudWatch receives and parses it.&lt;/li&gt;
&lt;li&gt;A scheduled log query detects a meaningful condition.&lt;/li&gt;
&lt;li&gt;The alarm invokes the agent with a signed request.&lt;/li&gt;
&lt;li&gt;The agent correlates the device event with AWS logs, traces, and recent changes.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Keep the alarm queries narrow enough that they indicate a real investigation. A noisy alert stream will train operators to ignore the integration. Include the log group, query, time window, and alarm reason in the payload so the agent has an evidence boundary rather than a vague instruction to investigate.&lt;/p&gt;
&lt;h2 id="watch-the-ingestion-pipeline"&gt;Watch the ingestion pipeline&lt;/h2&gt;
&lt;p&gt;Do not monitor only the final alarm. Monitor delivery metrics such as messages received and messages dropped, including the reason for drops. Truncation, invalid format, and malformed priority values should become visible before an incident depends on the feed.&lt;/p&gt;
&lt;p&gt;Also test the case where the AWS connection is unavailable. A device that queues locally can replay messages when the path returns. A device that sends UDP packets and forgets them cannot. The difference affects the confidence level an agent should attach to its diagnosis.&lt;/p&gt;
&lt;h2 id="a-sensible-rollout"&gt;A sensible rollout&lt;/h2&gt;
&lt;p&gt;Start with one device class and one hybrid service. Create a dedicated log group, configure the private endpoint, and validate a test message from the real network. Then compare the extracted fields with the raw message.&lt;/p&gt;
&lt;p&gt;After that, add one alarm for a high-signal event and connect it to a read-only DevOps Agent investigation. Verify that the agent cites the device event and the related AWS telemetry without inventing a causal link. Only then add more devices and more automated actions.&lt;/p&gt;
&lt;p&gt;The source article&amp;rsquo;s larger lesson is about evidence quality. CloudWatch can put on-premises logs beside AWS logs, but the operations team still owns retention, resilience, access, and interpretation. Once those foundations are tested, DevOps Agent can shorten the distance between a network event and a useful incident explanation.&lt;/p&gt;
&lt;p&gt;Hybrid operations become much easier to automate when the evidence is centralized, queryable, and honest about its gaps.&lt;/p&gt;</content:encoded></item><item><title>Faster S3 access log queries make storage security more usable</title><link>https://theawsblog.com/news/emiliano-montesdeoca/s3-access-logs-cloudwatch-s3-tables/</link><pubDate>Mon, 29 Jun 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/s3-access-logs-cloudwatch-s3-tables/</guid><description>AWS shows how CloudWatch and S3 Tables can make S3 access logs easier to query, which helps builders turn storage audit data into operational and security signals.</description><content:encoded>&lt;p&gt;S3 access logs are valuable, but value delayed often becomes value ignored. If logs are difficult to query, teams use them only after something has already gone wrong.&lt;/p&gt;
&lt;p&gt;The AWS Storage Blog post on &lt;a href="https://aws.amazon.com/blogs/storage/query-amazon-s3-access-logs-instantly-with-cloudwatch-and-s3-tables/"&gt;querying Amazon S3 access logs instantly with CloudWatch and S3 Tables&lt;/a&gt; is interesting because it focuses on making access data easier to use in everyday operations.&lt;/p&gt;
&lt;h2 id="what-changed"&gt;What changed&lt;/h2&gt;
&lt;p&gt;The article walks through delivering S3 access log data to CloudWatch Logs and S3 Tables, then using those destinations for dashboards, alarms, and queries. The practical shift is from &amp;ldquo;logs exist somewhere&amp;rdquo; to &amp;ldquo;logs are queryable enough to support investigation and monitoring.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;For builders, that means S3 access patterns can become part of normal observability instead of a separate forensic workflow.&lt;/p&gt;
&lt;h2 id="why-it-matters"&gt;Why it matters&lt;/h2&gt;
&lt;p&gt;S3 is often where the most important data lives. Application logs, analytics exports, customer files, model artifacts, backups, and operational reports all end up in buckets. Yet many teams still monitor bucket configuration more carefully than bucket access behavior.&lt;/p&gt;
&lt;p&gt;Queryable access logs help answer questions that matter:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Which principals are reading sensitive prefixes?&lt;/li&gt;
&lt;li&gt;Did access spike after a deployment?&lt;/li&gt;
&lt;li&gt;Are clients getting unexpected 403 or 404 responses?&lt;/li&gt;
&lt;li&gt;Which workloads are driving request cost?&lt;/li&gt;
&lt;li&gt;Did a suspicious IP enumerate objects?&lt;/li&gt;
&lt;li&gt;Are lifecycle or replication assumptions visible in traffic?&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;When these questions are cheap to answer, teams ask them earlier.&lt;/p&gt;
&lt;h2 id="the-trade-offs"&gt;The trade-offs&lt;/h2&gt;
&lt;p&gt;More logging is not free. S3 access log delivery, CloudWatch ingestion, query volume, table storage, retention, and dashboards all have cost implications. The right design depends on the bucket&amp;rsquo;s risk and traffic profile.&lt;/p&gt;
&lt;p&gt;I would not send every low-value development bucket into a high-retention analytics pipeline. I would prioritize buckets that contain customer data, security logs, production artifacts, financial exports, backups, or AI training and retrieval data.&lt;/p&gt;
&lt;p&gt;Also, access logs are only one layer. They should complement CloudTrail data events, IAM Access Analyzer, Macie, GuardDuty, S3 Storage Lens, and application-level audit logs. Different signals answer different questions.&lt;/p&gt;
&lt;h2 id="what-to-do-next"&gt;What to do next&lt;/h2&gt;
&lt;p&gt;Pick a production bucket that matters and define three operational queries before building anything. For example:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;top readers by prefix over the last hour,&lt;/li&gt;
&lt;li&gt;denied requests by principal and source IP,&lt;/li&gt;
&lt;li&gt;unusual data transfer volume compared with baseline.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Then build the logging path and dashboard around those questions. Add alarms only where the signal is actionable; noisy storage alarms quickly become invisible.&lt;/p&gt;
&lt;p&gt;The main takeaway is that storage observability should be designed for regular use, not just incident response. Faster S3 access log queries make that much more realistic.&lt;/p&gt;</content:encoded></item></channel></rss>