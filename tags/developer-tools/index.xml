<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Developer-Tools | The AWS Blog</title><link>https://theawsblog.com/tags/developer-tools/</link><description>Articles, tutorials and insights from the AWS community.</description><generator>Hugo</generator><language>en</language><managingEditor>@theawsblog (The AWS Blog)</managingEditor><webMaster>@theawsblog</webMaster><lastBuildDate>Fri, 07 Aug 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://theawsblog.com/tags/developer-tools/index.xml" rel="self" type="application/rss+xml"/><item><title>Portable Agent Plugins Need Strong Contracts, Not Just a Shared Folder</title><link>https://theawsblog.com/news/emiliano-montesdeoca/portable-agent-plugins-open-standard/</link><pubDate>Fri, 07 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/portable-agent-plugins-open-standard/</guid><description>The Agent Plugins standard can reduce repeated packaging work, but portability depends on explicit contracts for permissions, versions, lifecycle, and observability.</description><content:encoded>&lt;p&gt;Packaging the same agent extension for every client is wasteful. One tool expects a certain directory layout, another wants a different manifest, and a third wraps the same MCP server in its own configuration. The extension author ends up maintaining several copies of the same idea.&lt;/p&gt;
&lt;p&gt;AWS&amp;rsquo;s announcement of support for &lt;a href="https://aws.amazon.com/blogs/opensource/aws-supports-agent-plugins-an-open-standard-for-portable-agent-extensions/"&gt;Agent Plugins&lt;/a&gt; addresses that friction with an open standard for portable agent extensions. The initial scope covers Agent Skills and MCP servers in a shared plugin structure.&lt;/p&gt;
&lt;p&gt;That is a useful start. It is not the whole portability problem.&lt;/p&gt;
&lt;h2 id="what-the-standard-gets-right"&gt;What the standard gets right&lt;/h2&gt;
&lt;p&gt;A small standard is easier to implement and easier to govern. Agent Plugins defines a directory structure and manifest so a compatible client can discover skills and MCP server configuration without a bespoke adapter for each tool.&lt;/p&gt;
&lt;p&gt;The source article describes extension authors doing redundant work to adapt the same components for different clients. One portable package can reduce that duplication and give clients a common vocabulary. The involvement of multiple ecosystem participants also matters: an extension standard is more useful when it is not controlled by a single product roadmap.&lt;/p&gt;
&lt;p&gt;I like the decision to keep the first version narrow. A standard should define the minimum needed for interoperability and leave clients room to decide how they install, present, and manage extensions.&lt;/p&gt;
&lt;h2 id="the-hard-parts-are-contracts"&gt;The hard parts are contracts&lt;/h2&gt;
&lt;p&gt;A directory layout does not tell a client whether a tool is safe or reliable. Before shipping a plugin, document the contracts around it.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Permissions:&lt;/strong&gt; What files, network endpoints, APIs, and secrets does the plugin need? Can a client display those requirements before installation and enforce least privilege at runtime? An MCP server that can read an AWS credential or a local workspace needs a much clearer boundary than a static skill file.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Versioning:&lt;/strong&gt; A plugin can be structurally valid and still be incompatible with a client or a dependent server. Define how breaking changes, minimum client versions, and deprecation windows are communicated. A version field is not a compatibility strategy by itself.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Lifecycle:&lt;/strong&gt; What happens when an MCP server crashes, hangs, or loses authentication? The client needs a timeout, restart policy, and a useful error surface. Otherwise a portable plugin simply moves the debugging problem from packaging into runtime discovery.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Observability:&lt;/strong&gt; Every skill and tool invocation should have structured logs and correlation IDs. When a portable extension fails in one client but works in another, the team needs to compare tool inputs, timeouts, permissions, and returned schemas.&lt;/p&gt;
&lt;p&gt;These are the contracts that determine whether portability survives contact with production.&lt;/p&gt;
&lt;h2 id="test-more-than-installation"&gt;Test more than installation&lt;/h2&gt;
&lt;p&gt;A plugin should be tested in at least two compatible clients before anyone claims it is portable. Installation is the easy check. The interesting tests are:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;a missing permission,&lt;/li&gt;
&lt;li&gt;an unavailable MCP server,&lt;/li&gt;
&lt;li&gt;a tool timeout,&lt;/li&gt;
&lt;li&gt;a changed response schema,&lt;/li&gt;
&lt;li&gt;two skills calling the same tool concurrently,&lt;/li&gt;
&lt;li&gt;an update that must be rolled back.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;For each test, record what the user sees and what telemetry the operator receives. A portable extension that fails differently in every client is not yet portable in the operational sense.&lt;/p&gt;
&lt;p&gt;I would also keep the plugin&amp;rsquo;s domain logic outside the client-specific wrapper. Skills should document their assumptions, and MCP servers should expose stable, validated tool contracts. That makes it possible to reuse the same extension in a local developer tool and in a controlled enterprise agent runtime without copying business logic.&lt;/p&gt;
&lt;h2 id="where-the-ecosystem-should-go-next"&gt;Where the ecosystem should go next&lt;/h2&gt;
&lt;p&gt;The first version can provide a foundation, but future iterations will need stronger answers around permissions, trust, signing, sandboxing, lifecycle, and policy evaluation. Organizations will also want an inventory of installed plugins, their versions, their owners, and the data they accessed.&lt;/p&gt;
&lt;p&gt;Those requirements should not all be forced into the core standard immediately. They can evolve through documented conventions and interoperable metadata. What matters is that teams start treating an agent plugin like a deployable component, not like a harmless prompt file.&lt;/p&gt;
&lt;p&gt;The practical takeaway is positive but measured: Agent Plugins can stop authors from rebuilding the same extension for every client. The next value comes from the ecosystem agreeing on the contracts around that package. Prototype a plugin in two clients, declare its permissions and dependencies, instrument its tool calls, and report what breaks. That feedback is more useful than another compatibility badge.&lt;/p&gt;</content:encoded></item><item><title>Lambda runtime upgrades need campaigns, not reminders</title><link>https://theawsblog.com/news/emiliano-montesdeoca/lambda-runtime-upgrades-aws-transform/</link><pubDate>Sat, 20 Jun 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/lambda-runtime-upgrades-aws-transform/</guid><description>AWS Transform custom can help teams upgrade Lambda runtimes at scale, but the durable improvement is treating runtime changes as governed modernization campaigns.</description><content:encoded>&lt;p&gt;Lambda runtime upgrades are easy to postpone because the function still works today. Then a deprecation date becomes a support risk, a security exception, or a rushed platform campaign.&lt;/p&gt;
&lt;p&gt;The AWS Compute Blog post on &lt;a href="https://aws.amazon.com/blogs/compute/upgrading-lambda-function-runtimes-at-scale-with-aws-transform-custom/"&gt;upgrading Lambda function runtimes at scale with AWS Transform custom&lt;/a&gt; is useful because it treats runtime upgrades as a repeatable modernization workflow, not a calendar reminder.&lt;/p&gt;
&lt;h2 id="what-changed"&gt;What changed&lt;/h2&gt;
&lt;p&gt;AWS Transform custom can analyze codebases, identify runtime upgrade risk, apply transformations, update dependencies or configuration, and validate against exit criteria. The source article focuses on Lambda runtime upgrades, including cases where code changes are required, such as moving callback-style Node.js handlers toward async patterns.&lt;/p&gt;
&lt;p&gt;For platform teams, the more important piece is campaign management. A large organization rarely has one repository and one owner. It has hundreds of functions across many accounts, teams, IaC stacks, and CI/CD systems.&lt;/p&gt;
&lt;h2 id="why-builders-should-care"&gt;Why builders should care&lt;/h2&gt;
&lt;p&gt;Runtime upgrades fail when ownership is unclear.&lt;/p&gt;
&lt;p&gt;An application team may not know a function exists. A platform team may see the deprecated runtime but not understand the deployment path. Security may see the exposure but not own the code. The result is a spreadsheet-driven migration with too many manual exceptions.&lt;/p&gt;
&lt;p&gt;A transformation tool can help, but only if it plugs into an operating model:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;inventory functions and repositories,&lt;/li&gt;
&lt;li&gt;map functions to owners,&lt;/li&gt;
&lt;li&gt;assess code and dependency risk,&lt;/li&gt;
&lt;li&gt;create pull requests or transformation branches,&lt;/li&gt;
&lt;li&gt;validate with tests and alarms,&lt;/li&gt;
&lt;li&gt;deploy with safe rollout and rollback,&lt;/li&gt;
&lt;li&gt;track completion centrally.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;That is a campaign, not a one-off task.&lt;/p&gt;
&lt;h2 id="the-trade-offs"&gt;The trade-offs&lt;/h2&gt;
&lt;p&gt;Automated transformation is not a substitute for test coverage. It increases the value of tests because it can make changes faster than humans can manually review every edge case.&lt;/p&gt;
&lt;p&gt;Before trusting a runtime upgrade, teams should check:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;unit and integration tests for handler behavior,&lt;/li&gt;
&lt;li&gt;dependency compatibility with the target runtime,&lt;/li&gt;
&lt;li&gt;infrastructure definitions that set the runtime,&lt;/li&gt;
&lt;li&gt;packaging and build pipeline changes,&lt;/li&gt;
&lt;li&gt;observability after deployment,&lt;/li&gt;
&lt;li&gt;alias or version-based rollback strategy.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;For platform teams, the other trade-off is centralization. Pushing changes across many repositories can be efficient, but service owners still need to understand and approve behavior changes. The best model is usually centralized orchestration with distributed ownership.&lt;/p&gt;
&lt;h2 id="what-to-do-next"&gt;What to do next&lt;/h2&gt;
&lt;p&gt;Start with inventory. Use AWS Config, Trusted Advisor, CLI scripts, tags, or deployment metadata to find functions approaching deprecation. Then group them by owner, runtime, business criticality, and test readiness.&lt;/p&gt;
&lt;p&gt;For low-risk functions with good tests, an automated transform can move quickly. For high-risk functions with poor tests, the first transformation should probably add documentation, tests, or alarms before changing the runtime.&lt;/p&gt;
&lt;p&gt;The lesson from this AWS post is broader than Lambda. Modernization work becomes manageable when it is continuous, visible, and validated. If runtime upgrades are still handled as emergency cleanup, the tooling can help, but the process needs upgrading too.&lt;/p&gt;</content:encoded></item></channel></rss>