<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Bedrock | The AWS Blog</title><link>https://theawsblog.com/tags/bedrock/</link><description>Articles, tutorials and insights from the AWS community.</description><generator>Hugo</generator><language>en</language><managingEditor>@theawsblog (The AWS Blog)</managingEditor><webMaster>@theawsblog</webMaster><lastBuildDate>Fri, 07 Aug 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://theawsblog.com/tags/bedrock/index.xml" rel="self" type="application/rss+xml"/><item><title>Agent Authorization Needs to Understand Time and Sequence</title><link>https://theawsblog.com/news/emiliano-montesdeoca/bedrock-agentcore-temporal-policies/</link><pubDate>Fri, 07 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/bedrock-agentcore-temporal-policies/</guid><description>Amazon Bedrock AgentCore temporal policies extend authorization beyond identity so agent actions can be checked against workflow history, freshness, approvals, and cumulative limits.</description><content:encoded>&lt;p&gt;Traditional authorization asks who is making a request and whether that principal can call a tool. That model is still necessary, but agents create a second question: what happened immediately before this request?&lt;/p&gt;
&lt;p&gt;A single tool call may be harmless in isolation and dangerous in sequence. An agent can read an account, invent a different identifier, and pass it to a money-moving tool. It can reuse an old approval. It can execute many individually valid actions until the total crosses a business limit.&lt;/p&gt;
&lt;p&gt;The AWS Machine Learning Blog&amp;rsquo;s &lt;a href="https://aws.amazon.com/blogs/machine-learning/securing-ai-agents-with-temporal-policies-in-amazon-bedrock-agentcore/"&gt;temporal policy pattern for Amazon Bedrock AgentCore&lt;/a&gt; addresses that gap by evaluating an action against the agent&amp;rsquo;s session history. The important idea is not another policy syntax. It is that authorization for an agent needs to understand trajectory.&lt;/p&gt;
&lt;h2 id="identity-is-not-enough"&gt;Identity is not enough&lt;/h2&gt;
&lt;p&gt;A normal IAM check can answer whether an agent role is allowed to call &lt;code&gt;execute_trade&lt;/code&gt;. It does not necessarily know whether the agent fetched a current price, received approval for this trade, or already spent the session&amp;rsquo;s budget.&lt;/p&gt;
&lt;p&gt;Temporal policies can express conditions such as:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;a profile lookup must happen before a write action,&lt;/li&gt;
&lt;li&gt;the identifier used by the next tool must match a value returned earlier,&lt;/li&gt;
&lt;li&gt;a price must be refreshed within a defined time window,&lt;/li&gt;
&lt;li&gt;cumulative spend must stay below a session limit,&lt;/li&gt;
&lt;li&gt;a human approval can be consumed only once,&lt;/li&gt;
&lt;li&gt;a capability expires after inactivity.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;These are business and safety rules that depend on order, time, and accumulated state.&lt;/p&gt;
&lt;h2 id="put-the-enforcement-outside-the-agent"&gt;Put the enforcement outside the agent&lt;/h2&gt;
&lt;p&gt;The source article emphasizes that the policies run at the AgentCore Gateway perimeter, outside the agent&amp;rsquo;s own code. That placement is important. If the agent&amp;rsquo;s prompt, planner, or tool wrapper is the only enforcement point, a model mistake or a code defect can bypass it.&lt;/p&gt;
&lt;p&gt;The gateway still needs an accurate session boundary. Requests carry a session ID so the policy engine can associate actions with the right trajectory. Treat that ID as security-sensitive. A collision or a user-controlled identifier that is not scoped correctly can mix histories and produce the wrong authorization decision.&lt;/p&gt;
&lt;p&gt;Keep policy decisions observable. For every deny, operators should be able to see which condition failed, which previous action was relevant, and whether the problem was a legitimate workflow edge case or an attempted unsafe sequence.&lt;/p&gt;
&lt;h2 id="temporal-rules-are-least-privilege-with-context"&gt;Temporal rules are least privilege with context&lt;/h2&gt;
&lt;p&gt;Least privilege for agents is more precise than giving a role access to a list of tools. It can mean giving a tool access only after the agent has completed the required verification steps, for a limited time, and within a cumulative budget.&lt;/p&gt;
&lt;p&gt;That changes how I would design an agent workflow. First map the destructive or irreversible actions. Then identify the facts that must be true before each action. Those facts become policy conditions rather than instructions in a system prompt.&lt;/p&gt;
&lt;p&gt;For example, a support agent may read a customer record freely, but a refund tool could require a recent order lookup, a matching order ID, a maximum amount, and one human approval per refund. The agent can still plan and converse flexibly. The gateway controls the action boundary.&lt;/p&gt;
&lt;h2 id="start-in-observation-mode"&gt;Start in observation mode&lt;/h2&gt;
&lt;p&gt;Do not turn a complex temporal policy on in enforcement mode without seeing how real traffic behaves. Begin with a logging or observation mode if the integration supports it. Collect normal trajectories, denied-looking sequences, stale data cases, retries, and human handoffs.&lt;/p&gt;
&lt;p&gt;Then add one requirement at a time:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Define the session boundary and identity mapping.&lt;/li&gt;
&lt;li&gt;Inventory tool calls and mark which actions are consequential.&lt;/li&gt;
&lt;li&gt;Write a policy for one sequence or freshness rule.&lt;/li&gt;
&lt;li&gt;Replay legitimate and adversarial trajectories in tests.&lt;/li&gt;
&lt;li&gt;Observe production-like traffic without blocking it.&lt;/li&gt;
&lt;li&gt;Move the rule to enforcement after false positives are understood.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;A policy that is too strict can push operators toward disabling the control. A policy that is too vague gives false confidence. Small, testable rules are easier to review and explain.&lt;/p&gt;
&lt;h2 id="what-temporal-policies-do-not-solve"&gt;What temporal policies do not solve&lt;/h2&gt;
&lt;p&gt;They do not make a tool safe by themselves. The tool still needs input validation, idempotency, resource scoping, and useful errors. They also do not verify that the model&amp;rsquo;s natural-language explanation is true. They control whether an action is permitted under the recorded trajectory.&lt;/p&gt;
&lt;p&gt;That distinction matters when a workflow includes external systems or asynchronous callbacks. Decide what evidence is authoritative, how long it remains fresh, and what happens when an upstream result is delayed or duplicated.&lt;/p&gt;
&lt;p&gt;Identity-based access control remains the foundation. Temporal policies add the missing context for workflows where order and time change the meaning of an action.&lt;/p&gt;
&lt;p&gt;For teams putting agents near customer data, financial operations, or production changes, that is a practical upgrade in security thinking: do not ask only who can call the tool. Ask what must have happened before the tool call can be trusted.&lt;/p&gt;</content:encoded></item><item><title>Beyond the SDK Demo: Building Production-Ready .NET Agents with AgentCore</title><link>https://theawsblog.com/news/emiliano-montesdeoca/dotnet-ai-agents-bedrock-agentcore/</link><pubDate>Fri, 07 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/dotnet-ai-agents-bedrock-agentcore/</guid><description>Moving a .NET AI agent from an SDK sample to Amazon Bedrock AgentCore means making deliberate choices about streaming, sessions, testing, deployment, and portability.</description><content:encoded>&lt;p&gt;The first .NET AI agent is usually easy to demonstrate. Register a model client, add a handler, send a prompt, print the answer. The production version is where the interesting decisions begin: how do sessions stay separate, how do responses stream, how do you test without AWS credentials, and what exactly happens when the runtime scales or restarts?&lt;/p&gt;
&lt;p&gt;The AWS Developer Tools Blog&amp;rsquo;s &lt;a href="https://aws.amazon.com/blogs/developer/building-and-deploying-net-ai-agents-with-amazon-bedrock-agentcore/"&gt;AgentCore guide for .NET&lt;/a&gt; introduces &lt;code&gt;AWS.AgentCore.Hosting&lt;/code&gt; and the surrounding tooling for those concerns. The library &amp;ldquo;handles the operational concerns: scaling, session routing, health checking, and providing managed capabilities like conversation memory.&amp;rdquo; That is useful, but a hosting library cannot choose the boundaries of your application for you.&lt;/p&gt;
&lt;h2 id="choose-an-api-shape-that-leaves-room"&gt;Choose an API shape that leaves room&lt;/h2&gt;
&lt;p&gt;The package supports a source-generator experience and an extension-method experience in ASP.NET Core. The choice looks like syntax. It is really a choice about how much control the application will need around the handler.&lt;/p&gt;
&lt;p&gt;If the agent is a small, stable endpoint, generated wiring can keep the code compact. If the agent will need custom middleware, request correlation, policy checks, metrics, or dependency injection behavior, the extension method gives the team more room to shape the host. The source article describes middleware that can intercept every invocation. That is exactly where I would put cross-cutting concerns rather than scattering them through prompt handlers.&lt;/p&gt;
&lt;p&gt;Keep the handler thin either way. Put business operations, model selection, and external calls behind services that can be tested without the AgentCore runtime. Then the hosting layer remains an integration boundary, not the place where the entire application lives.&lt;/p&gt;
&lt;h2 id="streaming-changes-the-contract"&gt;Streaming changes the contract&lt;/h2&gt;
&lt;p&gt;Returning a complete string is comfortable, but it makes users wait for the slowest part of the generation. AgentCore supports returning &lt;code&gt;IAsyncEnumerable&amp;lt;string&amp;gt;&lt;/code&gt; so tokens can be streamed as they are produced. For conversational applications, the difference between first token latency and total response latency is visible immediately.&lt;/p&gt;
&lt;p&gt;Streaming also changes failure handling. A model call, memory lookup, or downstream tool can fail after the response has started. The caller cannot receive a new HTTP status code at that point, so the protocol needs a clear way to represent an error in the stream. Cancellation matters as well: when the browser disconnects, the handler should stop generating and release work instead of continuing to pay for a response nobody will read.&lt;/p&gt;
&lt;p&gt;Test partial output, cancellation, slow clients, and a failure after the first token. A streaming demo proves that bytes arrive. A production test proves that the system behaves when the stream is interrupted.&lt;/p&gt;
&lt;h2 id="session-ids-are-security-boundaries"&gt;Session IDs are security boundaries&lt;/h2&gt;
&lt;p&gt;AgentCore Memory combines a configured Memory ID with a session ID to load and store conversation history. That makes the session ID part of your security model, not just a convenience parameter.&lt;/p&gt;
&lt;p&gt;Two users must never share a session by accident. A missing session ID should fail clearly or create a deliberately scoped new session; it should not quietly fall back to a shared default. Add tests for two independent sessions, repeated calls in the same session, missing identifiers, and attempts to reuse another user&amp;rsquo;s identifier.&lt;/p&gt;
&lt;p&gt;The local emulator and &lt;code&gt;AWS.AgentCore.Testing&lt;/code&gt; package are valuable here. Use them to verify session behavior in CI without deploying the agent or requiring live AWS credentials. Memory integration is exactly the kind of feature that appears correct in a happy-path demo and fails in a multi-user system.&lt;/p&gt;
&lt;h2 id="deployment-is-still-architecture"&gt;Deployment is still architecture&lt;/h2&gt;
&lt;p&gt;The &lt;code&gt;dotnet aws deploy&lt;/code&gt; workflow makes deployment approachable, but it also hides choices. Container architecture, memory, VPC access, request header allowlists, and private service connectivity all affect runtime behavior.&lt;/p&gt;
&lt;p&gt;Read the generated resources and make those choices explicit. If the agent needs a private database or internal API, test the VPC path from the deployed runtime rather than assuming the local Aspire application proves it. If the service has a meaningful cold-start budget, measure it on the actual architecture you plan to run.&lt;/p&gt;
&lt;p&gt;Native AOT can reduce startup time, but it changes the development experience. Source-generated serialization and explicit service resolution may be required where flexible reflection-based binding previously worked. Decide whether cold-start reduction is worth that constraint for this workload instead of enabling AOT because it is available.&lt;/p&gt;
&lt;h2 id="keep-portability-in-the-business-layer"&gt;Keep portability in the business layer&lt;/h2&gt;
&lt;p&gt;AgentCore is a managed runtime, but the agent&amp;rsquo;s business logic can remain portable. Treat the handler as an adapter around services that know how to perform domain operations. Avoid spreading AgentCore-specific session and transport calls through every class.&lt;/p&gt;
&lt;p&gt;That design gives you a useful fallback: the same domain service can be hosted in another ASP.NET Core application, tested in isolation, or moved to another container platform if the operational requirement changes. Portability is not free, but it is much cheaper when the boundary is intentional from the beginning.&lt;/p&gt;
&lt;h2 id="my-pre-production-checklist"&gt;My pre-production checklist&lt;/h2&gt;
&lt;p&gt;Before the first real users arrive, I would require:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;separate-session tests and a clear session ID ownership model,&lt;/li&gt;
&lt;li&gt;streaming tests covering cancellation and mid-response failures,&lt;/li&gt;
&lt;li&gt;emulator-backed integration tests in CI,&lt;/li&gt;
&lt;li&gt;health and correlation telemetry around every invocation,&lt;/li&gt;
&lt;li&gt;a documented deployment and rollback path,&lt;/li&gt;
&lt;li&gt;a decision on JIT versus Native AOT based on measured startup data.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The AgentCore hosting library removes a lot of plumbing. That is its value. The remaining work is the architecture around the plumbing: state, failure, identity, and operations. The SDK demo gets you to the first response. Those decisions are what get the agent to production.&lt;/p&gt;</content:encoded></item><item><title>Persistent Compute Changes the Bedrock AgentCore Boundary</title><link>https://theawsblog.com/news/emiliano-montesdeoca/bedrock-agentcore-runtime-instances-persistent-compute/</link><pubDate>Fri, 07 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/bedrock-agentcore-runtime-instances-persistent-compute/</guid><description>Amazon Bedrock AgentCore runtime instances bring persistent sessions and shared compute to agent workloads, changing the trade-off between stateless scale and durable execution.</description><content:encoded>&lt;p&gt;For a long time, the cleanest way to think about an AI agent was as a request handler: invoke it, let it do some work, return a result, and put durable state somewhere else. That model is excellent for short tasks. It becomes awkward when an agent needs a shared workspace, a GPU, or a session that stays alive across several days.&lt;/p&gt;
&lt;p&gt;&lt;a href="https://aws.amazon.com/blogs/aws/runtime-instances-persistent-compute-for-production-ai-agents-on-amazon-bedrock-agentcore/"&gt;Amazon Bedrock AgentCore runtime instances&lt;/a&gt; change that boundary. They add persistent, AWS-managed compute for production agents while keeping the AgentCore operational model. The useful part is not simply that an instance can stay alive. It is that a group of agents can share a session and local state without every interaction becoming another distributed-systems problem.&lt;/p&gt;
&lt;h2 id="what-changed"&gt;What changed&lt;/h2&gt;
&lt;p&gt;The stateless AgentCore runtime model behaves much like a short-lived service. It can scale quickly, but the work in one invocation is not a durable workspace. Runtime instances provide a persistent session on managed EC2 infrastructure. The source article describes sessions that can last up to 14 days, be stopped and restarted, share a file system, and use GPU-backed instances when the workload needs them.&lt;/p&gt;
&lt;p&gt;That opens patterns such as a code-writing agent and a review agent working against the same files, or an analysis agent keeping a prepared workspace between stages. Agents can coordinate through tools, while the session preserves the local context that would otherwise need to move through APIs or external storage.&lt;/p&gt;
&lt;p&gt;This is a meaningful shift. A runtime instance session is closer to a durable compute workspace than to a single model request.&lt;/p&gt;
&lt;h2 id="the-trade-off-is-operational-weight"&gt;The trade-off is operational weight&lt;/h2&gt;
&lt;p&gt;Persistent state is convenient, but it is not free. With a stateless runtime, scaling and isolation are relatively easy to reason about. With a persistent instance, you need to understand what happens when the instance stops, restarts, becomes unhealthy, or runs out of memory.&lt;/p&gt;
&lt;p&gt;The first trade-off is state durability. A shared session directory is useful for coordination, but it should not be your only copy of important work. Use durable stores such as Amazon S3, EBS snapshots, or AgentCore Memory according to the kind of state you are protecting. Working files, conversation memory, and business records have different durability requirements.&lt;/p&gt;
&lt;p&gt;The second trade-off is isolation. Multiple agents sharing an instance can reduce network chatter and improve collaboration, but they also share compute, memory, and local storage. A runaway process can affect the rest of the session. A poorly designed file workflow can let one agent overwrite another agent&amp;rsquo;s work. Use explicit directories, atomic writes, quotas, and clear ownership rules instead of assuming that a shared workspace is automatically safe.&lt;/p&gt;
&lt;p&gt;The third trade-off is cost. Stateless runtimes scale to zero between requests. An instance is an hourly compute commitment while it is running. Stopping a session can reduce cost, but it also introduces startup latency. The economics favor workloads with meaningful periods of continuous work, such as batch analysis, code generation, simulation, or long-running agent collaboration. They are less obvious for sporadic chat traffic.&lt;/p&gt;
&lt;h2 id="a-hybrid-shape-makes-sense"&gt;A hybrid shape makes sense&lt;/h2&gt;
&lt;p&gt;I do not see runtime instances as a replacement for stateless runtimes. The more interesting architecture uses both.&lt;/p&gt;
&lt;p&gt;A lightweight front end or orchestrator can handle authentication, request routing, and short interactions on a fast-scaling runtime. It can then delegate expensive or stateful work to a runtime instance. That lets the stateless part absorb bursts while the persistent part handles a bounded session with a known lifecycle.&lt;/p&gt;
&lt;p&gt;The boundary should be explicit. Decide which data belongs in the session, which data must survive the session, and which actions require an external system of record. Do not let a local file quietly become the database because it was convenient during the prototype.&lt;/p&gt;
&lt;h2 id="what-i-would-test-first"&gt;What I would test first&lt;/h2&gt;
&lt;p&gt;Before moving a serious workload, I would run a day-long pilot and measure the uncomfortable details:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Stop a session and restart it later. Verify which files, processes, and memory records survive.&lt;/li&gt;
&lt;li&gt;Run two agents that write to the same workspace and test contention, partial writes, and recovery.&lt;/li&gt;
&lt;li&gt;Create a memory-pressure test. Confirm that one agent cannot starve the rest of the session.&lt;/li&gt;
&lt;li&gt;Compare the hourly cost of a warm instance with the invocation and storage cost of a stateless design.&lt;/li&gt;
&lt;li&gt;Measure provisioning time when scaling from one session to several concurrent sessions.&lt;/li&gt;
&lt;li&gt;Add correlation by session ID to logs, traces, and agent-to-agent calls before the pilot grows.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The source article calls out persistent sessions, shared storage, multi-agent coordination, and GPU access as the main capabilities. Those are useful primitives, but they do not remove the design work around lifecycle, isolation, and recovery.&lt;/p&gt;
&lt;p&gt;The practical takeaway is simple: runtime instances make durable agent workspaces a first-class option on AWS. Use them when the workload genuinely benefits from continuity. Keep short-lived work stateless, keep business state durable, and make the session lifecycle part of the architecture rather than an implementation detail.&lt;/p&gt;</content:encoded></item><item><title>Real-Time Voice AI Is a Session Coordination Problem</title><link>https://theawsblog.com/news/emiliano-montesdeoca/serverless-real-time-voice-ai/</link><pubDate>Fri, 07 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/serverless-real-time-voice-ai/</guid><description>A serverless voice AI pattern with Amazon Bedrock AgentCore and AppSync Events shows that audio transport, liveness, session state, and observability matter as much as model latency.</description><content:encoded>&lt;p&gt;Voice AI discussions often begin with the model. Which model is fastest? Which one sounds most natural? Which one costs less? Those questions matter, but a production voice experience usually fails somewhere else: audio transport, session state, container liveness, or a reconnect path nobody tested.&lt;/p&gt;
&lt;p&gt;The AWS Industries Blog&amp;rsquo;s &lt;a href="https://aws.amazon.com/blogs/industries/serverless-real-time-voice-ai-on-aws-a-pattern-for-enterprise-sales-coaching/"&gt;serverless real-time voice AI pattern&lt;/a&gt; uses Amazon Bedrock AgentCore and AWS AppSync Events for an enterprise sales-coaching scenario. The reusable lesson is architectural. Real-time voice is a coordination problem before it is a model-selection problem.&lt;/p&gt;
&lt;h2 id="separate-publishing-from-subscribing"&gt;Separate publishing from subscribing&lt;/h2&gt;
&lt;p&gt;The pattern uses AppSync Events as a serverless publish and subscribe layer for audio. A client publishes incoming audio to a session channel, while the voice-processing container publishes synthesized audio to an outgoing channel. The client subscribes to that outgoing channel over a persistent connection.&lt;/p&gt;
&lt;p&gt;This separates two concerns that are often tangled in a custom WebSocket server:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;publishing audio chunks into the session,&lt;/li&gt;
&lt;li&gt;maintaining a subscription that receives responses.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The backend does not need to own every client connection directly. AppSync Events handles the fanout, while the AgentCore container can focus on forwarding audio to the model and returning the generated response.&lt;/p&gt;
&lt;p&gt;That is attractive for intermittent workloads because the transport layer can scale independently of the voice-processing compute. The source article also highlights a low per-session event cost, but the larger benefit is avoiding an always-on connection tier for a workload that may be quiet between coaching sessions.&lt;/p&gt;
&lt;h2 id="liveness-is-part-of-the-product"&gt;Liveness is part of the product&lt;/h2&gt;
&lt;p&gt;The most memorable failure in the source article is not a bad transcript. Sessions ended after roughly two minutes because the container did not satisfy the runtime&amp;rsquo;s health expectations and the WebSocket endpoint was incorrect.&lt;/p&gt;
&lt;p&gt;That is a useful warning. A voice container can appear healthy from the outside while receiving no audio at all. If the liveness endpoint, session counters, and subscription status do not agree, the platform may make the wrong decision about an idle or unhealthy process.&lt;/p&gt;
&lt;p&gt;Implement the required health endpoint before wiring in the model. Keep liveness separate from business activity. A container should be able to say it is alive even when no user is speaking, and it should expose a separate signal for whether the audio subscription is connected and receiving events.&lt;/p&gt;
&lt;p&gt;Log the session ID, input chunk count, output chunk count, subscription state, and last successful model interaction. Exact-duration failures are a clue that lifecycle management, not model quality, is the problem.&lt;/p&gt;
&lt;h2 id="audio-latency-is-end-to-end"&gt;Audio latency is end to end&lt;/h2&gt;
&lt;p&gt;Model latency is only one part of the experience. The system also pays for client capture, encoding, publication, subscription delivery, model processing, speech generation, and playback buffering.&lt;/p&gt;
&lt;p&gt;A fast model behind a slow channel still feels slow. Measure timestamps at each boundary. Decide how much audio to batch: tiny chunks reduce interaction delay but increase event volume and overhead, while large chunks improve efficiency but make the conversation feel less responsive.&lt;/p&gt;
&lt;p&gt;Treat backpressure as a first-class case. If generated audio arrives faster than the browser can play it, the system needs a bounded buffer and a policy for dropping or slowing output. If the client pauses, the backend should not grow memory without limit.&lt;/p&gt;
&lt;h2 id="privacy-and-cost-need-evidence"&gt;Privacy and cost need evidence&lt;/h2&gt;
&lt;p&gt;Voice data carries sensitive content, especially in sales coaching or support scenarios. Define retention before launch. Decide whether raw audio, transcripts, prompts, and model outputs need to be stored at all, and restrict access to each artifact separately.&lt;/p&gt;
&lt;p&gt;Cost estimates based on a five-minute session are helpful for planning, but production costs include model usage, container runtime, event operations, logs, and failed or abandoned sessions. Add alarms for sessions that stay open without receiving audio. A disconnected client that leaves a container running is both a user-experience issue and a bill.&lt;/p&gt;
&lt;h2 id="a-practical-pilot"&gt;A practical pilot&lt;/h2&gt;
&lt;p&gt;I would test this pattern with one narrow workflow and synthetic audio first:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Verify bidirectional audio with a fixed test signal before adding the model.&lt;/li&gt;
&lt;li&gt;Kill the WebSocket subscription and confirm the session reconnects or closes cleanly.&lt;/li&gt;
&lt;li&gt;Stop the container during a session and document what the user hears.&lt;/li&gt;
&lt;li&gt;Test slow clients, duplicate events, out-of-order messages, and a full output buffer.&lt;/li&gt;
&lt;li&gt;Exercise the health endpoint while the session is idle and while it is busy.&lt;/li&gt;
&lt;li&gt;Measure first-audio latency, end-to-end response latency, event count, and cost per session.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The architecture is compelling because AppSync Events and AgentCore remove a lot of connection and lifecycle infrastructure. They do not remove the need to observe the boundaries between those services.&lt;/p&gt;
&lt;p&gt;The practical takeaway is that real-time voice AI is a distributed session. Design the audio channels, liveness signals, buffers, privacy controls, and reconnect behavior with the same care as the model prompt. That is how a demo becomes a conversation people can actually use.&lt;/p&gt;</content:encoded></item><item><title>Agent UIs need protocols, not only chat boxes</title><link>https://theawsblog.com/news/emiliano-montesdeoca/agentcore-agui-generative-ui/</link><pubDate>Fri, 17 Jul 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/agentcore-agui-generative-ui/</guid><description>AG-UI with Amazon Bedrock AgentCore shows how agent frontends can support generative UI, shared state, and human-in-the-loop workflows without coupling every frontend to one agent framework.</description><content:encoded>&lt;p&gt;A chat box is not enough for many agent workflows. Agents need to show charts, update state, ask for approval, stream intermediate results, and let users steer the task while it is running.&lt;/p&gt;
&lt;p&gt;The AWS Machine Learning Blog post on &lt;a href="https://aws.amazon.com/blogs/machine-learning/build-generative-ui-for-ai-agents-on-amazon-bedrock-agentcore-with-the-ag-ui-protocol/"&gt;building generative UI for AI agents on Amazon Bedrock AgentCore with AG-UI&lt;/a&gt; is useful because it treats agent user experience as a protocol problem.&lt;/p&gt;
&lt;h2 id="what-changed"&gt;What changed&lt;/h2&gt;
&lt;p&gt;AG-UI defines a standard event protocol between agent backends and frontends. The source article shows AG-UI running with Amazon Bedrock AgentCore Runtime, the FAST starter template, Strands Agents, LangGraph, Cognito, Amplify, and CopilotKit.&lt;/p&gt;
&lt;p&gt;The important idea is decoupling. The frontend should not need to know every detail of the agent framework. The agent should emit structured UI events that the frontend can render consistently.&lt;/p&gt;
&lt;h2 id="why-builders-should-care"&gt;Why builders should care&lt;/h2&gt;
&lt;p&gt;As agents move beyond Q&amp;amp;A, the UI becomes part of the system&amp;rsquo;s reliability and trust model.&lt;/p&gt;
&lt;p&gt;A human approval step should be explicit. A generated chart should be tied to data. A shared todo canvas should update predictably. A long-running agent should expose progress instead of leaving the user guessing.&lt;/p&gt;
&lt;p&gt;Protocols like AG-UI can make those interactions more portable across frameworks and frontends.&lt;/p&gt;
&lt;h2 id="the-trade-offs"&gt;The trade-offs&lt;/h2&gt;
&lt;p&gt;Generative UI introduces new failure modes. The agent can request the wrong component, produce inconsistent state, or ask for approval without enough context. Frontends need validation, permissions, and safe rendering rules.&lt;/p&gt;
&lt;p&gt;Builders should define which UI events are allowed, which components are trusted, and how user actions flow back to the agent. Treat UI actions like tool calls: validate inputs, authorize state changes, and log important decisions.&lt;/p&gt;
&lt;p&gt;Also avoid overbuilding. Some agents only need text. Use generative UI when interaction complexity justifies it.&lt;/p&gt;
&lt;h2 id="what-to-do-next"&gt;What to do next&lt;/h2&gt;
&lt;p&gt;Review agent workflows and identify where plain chat creates friction:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;approval gates,&lt;/li&gt;
&lt;li&gt;charts or tables,&lt;/li&gt;
&lt;li&gt;editing structured state,&lt;/li&gt;
&lt;li&gt;multi-step progress,&lt;/li&gt;
&lt;li&gt;choosing between alternatives,&lt;/li&gt;
&lt;li&gt;reviewing generated artifacts.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Those are good candidates for AG-UI-style events.&lt;/p&gt;
&lt;p&gt;The practical takeaway: the next generation of agent applications will need richer interaction contracts. A protocol boundary between agent logic and frontend rendering makes that easier to build and safer to operate.&lt;/p&gt;</content:encoded></item><item><title>Secure multi-tenant RAG needs authorization at retrieval time</title><link>https://theawsblog.com/news/emiliano-montesdeoca/secure-multitenant-rag-bedrock-verified-permissions/</link><pubDate>Tue, 14 Jul 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/secure-multitenant-rag-bedrock-verified-permissions/</guid><description>AWS shows how Amazon Bedrock Knowledge Bases and Verified Permissions can enforce runtime document access for intra-tenant RAG applications without duplicating every knowledge base.</description><content:encoded>&lt;p&gt;RAG security is not solved when the documents are ingested. It is solved when the right user retrieves the right document at the right time.&lt;/p&gt;
&lt;p&gt;The AWS Architecture Blog post on &lt;a href="https://aws.amazon.com/blogs/architecture/secure-multi-tenant-rag-with-amazon-bedrock-and-verified-permissions/"&gt;secure multi-tenant RAG with Amazon Bedrock and Verified Permissions&lt;/a&gt; is useful because it focuses on runtime authorization. The pattern uses a shared Bedrock knowledge base with metadata filtering, while Amazon Verified Permissions decides which filter should be applied for the current user.&lt;/p&gt;
&lt;p&gt;That is a practical middle ground for enterprises that need department-level access control inside one organization.&lt;/p&gt;
&lt;h2 id="what-changed"&gt;What changed&lt;/h2&gt;
&lt;p&gt;The source article describes a defense-in-depth pattern for intra-tenant RAG. Instead of hardcoding retrieval filters in application code, the application evaluates Cedar policies in Verified Permissions and constructs the metadata filter passed to the Bedrock retrieve-and-generate flow.&lt;/p&gt;
&lt;p&gt;That means access rules can change without redeploying the application. It also means authorization decisions can be audited outside the application code.&lt;/p&gt;
&lt;h2 id="why-builders-should-care"&gt;Why builders should care&lt;/h2&gt;
&lt;p&gt;Many internal RAG systems start with a simple permission model: each department gets a folder, each folder maps to a filter, and the app passes the filter during retrieval. That works until executives need cross-department access, project teams form, policies change, or audit teams ask where decisions are recorded.&lt;/p&gt;
&lt;p&gt;Externalized authorization helps because access logic becomes a policy layer, not a code branch.&lt;/p&gt;
&lt;h2 id="the-trade-offs"&gt;The trade-offs&lt;/h2&gt;
&lt;p&gt;The source article is clear about an important boundary: this is logical isolation, not hard infrastructure isolation. A shared knowledge base with metadata filters is not the same as a separate knowledge base per customer, account, or compliance boundary.&lt;/p&gt;
&lt;p&gt;Use this pattern when users are inside the same tenant or organization and the goal is department, role, or project-level control. Do not use it as the only boundary between separate customers where a filter bug would create a regulatory incident.&lt;/p&gt;
&lt;p&gt;Also design for failure. If the authorization service is unavailable, the safe default should be deny, not retrieve broadly.&lt;/p&gt;
&lt;h2 id="what-to-do-next"&gt;What to do next&lt;/h2&gt;
&lt;p&gt;Before building a RAG app, classify the isolation requirement:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;same organization, department-level permissions,&lt;/li&gt;
&lt;li&gt;separate business units with strong governance needs,&lt;/li&gt;
&lt;li&gt;external customers requiring hard tenant isolation,&lt;/li&gt;
&lt;li&gt;regulated data requiring infrastructure separation.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Then choose the architecture. A shared knowledge base plus Verified Permissions can be efficient for intra-tenant access. Dedicated knowledge bases or accounts may be necessary for hard boundaries.&lt;/p&gt;
&lt;p&gt;The practical takeaway: RAG authorization belongs in the retrieval path. If the model can see data the user should not see, the answer is already compromised.&lt;/p&gt;</content:encoded></item><item><title>Bedrock resilience patterns make LLM availability an architecture concern</title><link>https://theawsblog.com/news/emiliano-montesdeoca/bedrock-llm-gateway-resilience-patterns/</link><pubDate>Thu, 09 Jul 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/bedrock-llm-gateway-resilience-patterns/</guid><description>AWS guidance on Amazon Bedrock and LLM gateway resilience shows why production generative AI systems need quota isolation, cross-Region routing, and multi-model fallback design.</description><content:encoded>&lt;p&gt;A production LLM system can fail even when the application code is healthy. The model can throttle, a Region can have pressure, a quota can be exhausted, or one tenant can consume more shared capacity than expected.&lt;/p&gt;
&lt;p&gt;The AWS Machine Learning Blog post on &lt;a href="https://aws.amazon.com/blogs/machine-learning/implementing-resilience-patterns-with-amazon-bedrock-and-llm-gateway/"&gt;resilience patterns with Amazon Bedrock and an LLM gateway&lt;/a&gt; is useful because it treats model inference as production infrastructure, not a simple API dependency.&lt;/p&gt;
&lt;h2 id="what-changed"&gt;What changed&lt;/h2&gt;
&lt;p&gt;The source article walks through patterns from native Bedrock cross-Region inference to multi-account isolation and LLM gateway orchestration. The focus is availability, with cost, latency, and throughput acknowledged as connected design dimensions.&lt;/p&gt;
&lt;p&gt;That framing matters. For generative AI, reliability is not only uptime. It includes quota behavior, time to first token, time to last token, model availability, provider fallback, and tenant isolation.&lt;/p&gt;
&lt;h2 id="why-builders-should-care"&gt;Why builders should care&lt;/h2&gt;
&lt;p&gt;Many teams start with one model in one account in one Region. That is fine for experiments. It is fragile for production.&lt;/p&gt;
&lt;p&gt;As usage grows, the failure modes change:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;traffic spikes hit account or model quotas,&lt;/li&gt;
&lt;li&gt;a single tenant creates noisy-neighbor effects,&lt;/li&gt;
&lt;li&gt;regional routing affects latency and data residency,&lt;/li&gt;
&lt;li&gt;fallback models produce different output quality,&lt;/li&gt;
&lt;li&gt;retries increase cost and pressure during incidents.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Bedrock cross-Region inference can help absorb regional and quota pressure. Account sharding can create stronger isolation. An LLM gateway can centralize routing, fallback, rate limits, and policy.&lt;/p&gt;
&lt;h2 id="the-trade-offs"&gt;The trade-offs&lt;/h2&gt;
&lt;p&gt;Resilience patterns add complexity. Cross-Region routing can increase latency. Multi-account routing adds identity, billing, and observability work. Multi-model fallback can change output quality, safety behavior, and prompt compatibility.&lt;/p&gt;
&lt;p&gt;Builders should define acceptable degradation. If the primary model is unavailable, is it acceptable to use a cheaper or smaller model? Should the system return a partial answer? Should it queue the request? Should some tenants have reserved capacity while others use best-effort routing?&lt;/p&gt;
&lt;p&gt;Those are product decisions as much as infrastructure decisions.&lt;/p&gt;
&lt;h2 id="what-to-do-next"&gt;What to do next&lt;/h2&gt;
&lt;p&gt;Map each AI workload by business criticality:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;interactive user-facing flows,&lt;/li&gt;
&lt;li&gt;internal productivity tools,&lt;/li&gt;
&lt;li&gt;batch summarization,&lt;/li&gt;
&lt;li&gt;agent workflows,&lt;/li&gt;
&lt;li&gt;compliance-sensitive processing.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Then define resilience targets for each. A user-facing support agent may need low latency and graceful fallback. A nightly summarization job may tolerate delay but not data residency changes. A high-value tenant may need quota isolation.&lt;/p&gt;
&lt;p&gt;The practical takeaway: LLM availability should be designed explicitly. Bedrock gives useful primitives, but builders need routing, evaluation, and fallback policies that match the product&amp;rsquo;s tolerance for delay, cost, and output variation.&lt;/p&gt;</content:encoded></item><item><title>Voice analytics needs durable orchestration more than another demo pipeline</title><link>https://theawsblog.com/news/emiliano-montesdeoca/voice-analytics-durable-functions-bedrock/</link><pubDate>Wed, 08 Jul 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/voice-analytics-durable-functions-bedrock/</guid><description>AWS Lambda durable functions and Amazon Bedrock can simplify voice analytics workflows, especially when transcription, summarization, sentiment, and storage need reliable multi-step orchestration.</description><content:encoded>&lt;p&gt;Contact center AI is useful only if the workflow is reliable. Summaries, sentiment, and key topics are valuable, but missed segments, duplicate processing, or partial results quickly destroy trust.&lt;/p&gt;
&lt;p&gt;The AWS Compute Blog post on &lt;a href="https://aws.amazon.com/blogs/compute/build-reliable-voice-analytics-workflows-with-aws-lambda-durable-functions-and-amazon-bedrock/"&gt;reliable voice analytics workflows with Lambda durable functions and Amazon Bedrock&lt;/a&gt; is practical because it focuses on orchestration, not just model output.&lt;/p&gt;
&lt;h2 id="what-changed"&gt;What changed&lt;/h2&gt;
&lt;p&gt;The source architecture uses Kinesis Streams, DynamoDB, Lambda durable functions, Bedrock, API Gateway, Cognito, ECS, and a web application to process voice transcription segments and generate insights.&lt;/p&gt;
&lt;p&gt;The durable function waits until all transcription segments for a call are available, then orchestrates summarization, sentiment analysis, key-topic extraction, and persistence.&lt;/p&gt;
&lt;p&gt;The important part is that the workflow is stateful and checkpointed. Voice analytics is not a single API call. It is a multi-step data pipeline with ordering, completeness, retries, and user access concerns.&lt;/p&gt;
&lt;h2 id="why-builders-should-care"&gt;Why builders should care&lt;/h2&gt;
&lt;p&gt;Many teams prototype voice analytics by sending a transcript to a model and displaying the result. Production is harder:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;transcripts arrive in segments,&lt;/li&gt;
&lt;li&gt;calls can be long,&lt;/li&gt;
&lt;li&gt;model calls can fail or throttle,&lt;/li&gt;
&lt;li&gt;users need authorization to view results,&lt;/li&gt;
&lt;li&gt;data must be retained and searchable,&lt;/li&gt;
&lt;li&gt;duplicate processing can create conflicting insights,&lt;/li&gt;
&lt;li&gt;partial transcripts should not produce final summaries.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Lambda durable functions are a good fit when the workflow is code-owned and needs checkpointing without assembling custom orchestration infrastructure.&lt;/p&gt;
&lt;h2 id="the-trade-offs"&gt;The trade-offs&lt;/h2&gt;
&lt;p&gt;A durable serverless workflow still needs product decisions.&lt;/p&gt;
&lt;p&gt;Define when a transcript is complete. Decide how to handle late segments. Add idempotency for insight generation. Store model inputs and outputs carefully for audit and improvement. Protect call data with least privilege and encryption. Decide whether human review is needed before insights affect agent coaching or customer records.&lt;/p&gt;
&lt;p&gt;Also watch cost. Voice analytics can multiply model calls quickly if every call produces multiple insights, retries, and reprocessing jobs.&lt;/p&gt;
&lt;h2 id="what-to-do-next"&gt;What to do next&lt;/h2&gt;
&lt;p&gt;Before building the UI, define the workflow contract:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;What event starts processing?&lt;/li&gt;
&lt;li&gt;How is transcript completeness detected?&lt;/li&gt;
&lt;li&gt;Which Bedrock calls are required?&lt;/li&gt;
&lt;li&gt;What happens on failure or timeout?&lt;/li&gt;
&lt;li&gt;Who can view each transcript and insight?&lt;/li&gt;
&lt;li&gt;How are results corrected or regenerated?&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The practical takeaway: reliable voice analytics is an orchestration problem first and an AI problem second. Durable functions can help make the orchestration explicit enough to operate.&lt;/p&gt;</content:encoded></item><item><title>Claude Sonnet 5 on Bedrock should trigger model selection reviews</title><link>https://theawsblog.com/news/emiliano-montesdeoca/claude-sonnet-5-bedrock-model-selection/</link><pubDate>Tue, 07 Jul 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/claude-sonnet-5-bedrock-model-selection/</guid><description>Claude Sonnet 5 availability on Amazon Bedrock gives builders another production model option, but adoption should be driven by evaluation, cost, latency, and agent reliability.</description><content:encoded>&lt;p&gt;New model availability is exciting, but production teams should treat it as a reason to evaluate, not as a reason to immediately migrate.&lt;/p&gt;
&lt;p&gt;AWS announced &lt;a href="https://aws.amazon.com/blogs/machine-learning/introducing-claude-sonnet-5-on-aws-anthropics-most-capable-sonnet-model/"&gt;Claude Sonnet 5 on Amazon Bedrock and Claude Platform on AWS&lt;/a&gt;. The source article positions the model for coding, agents, professional work, and everyday high-scale use where teams want strong capability without always paying for the highest-tier model.&lt;/p&gt;
&lt;p&gt;For builders, the right question is: where does this model improve the product enough to justify the change?&lt;/p&gt;
&lt;h2 id="what-changed"&gt;What changed&lt;/h2&gt;
&lt;p&gt;Claude Sonnet 5 is available through Amazon Bedrock, with programmatic access through familiar Bedrock APIs and Anthropic SDK patterns. It is also available through Claude Platform on AWS for teams that want Anthropic&amp;rsquo;s native experience with AWS billing and authentication.&lt;/p&gt;
&lt;p&gt;The announcement emphasizes stronger coding, multi-step agent behavior, structured professional work, and production inference integration.&lt;/p&gt;
&lt;h2 id="why-builders-should-care"&gt;Why builders should care&lt;/h2&gt;
&lt;p&gt;Many teams now run multiple LLM workloads:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;chat assistance,&lt;/li&gt;
&lt;li&gt;code generation,&lt;/li&gt;
&lt;li&gt;document analysis,&lt;/li&gt;
&lt;li&gt;agent tool use,&lt;/li&gt;
&lt;li&gt;extraction pipelines,&lt;/li&gt;
&lt;li&gt;customer support automation,&lt;/li&gt;
&lt;li&gt;internal operations agents.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;A new model can improve one workload while being unnecessary for another. The best teams maintain a model portfolio, not a single default.&lt;/p&gt;
&lt;p&gt;Sonnet 5 may be especially interesting where the current model struggles to hold a plan, complete multi-file code changes, follow tool-use instructions, or produce consistent structured outputs.&lt;/p&gt;
&lt;h2 id="the-trade-offs"&gt;The trade-offs&lt;/h2&gt;
&lt;p&gt;Better capability does not automatically mean better production behavior.&lt;/p&gt;
&lt;p&gt;Evaluate:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;task success rate,&lt;/li&gt;
&lt;li&gt;latency and time to first token,&lt;/li&gt;
&lt;li&gt;cost per completed task,&lt;/li&gt;
&lt;li&gt;tool-call correctness,&lt;/li&gt;
&lt;li&gt;refusal and safety behavior,&lt;/li&gt;
&lt;li&gt;context handling,&lt;/li&gt;
&lt;li&gt;output schema reliability,&lt;/li&gt;
&lt;li&gt;regional availability,&lt;/li&gt;
&lt;li&gt;quota and throughput needs.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;For agents, evaluate end-to-end task completion, not isolated prompt quality. A model that writes a better single answer may still be worse for a tool-using workflow if it calls tools too often, skips validation, or produces brittle plans.&lt;/p&gt;
&lt;h2 id="what-to-do-next"&gt;What to do next&lt;/h2&gt;
&lt;p&gt;Create a small benchmark set from real failures. Include prompts where the current model needed human correction, broke a schema, chose the wrong tool, or produced incomplete code.&lt;/p&gt;
&lt;p&gt;Then compare Sonnet 5 against the current production model using the same system prompt, tools, data access, and evaluation criteria. If it wins, roll it out to the workload that benefits most, not to every workload by default.&lt;/p&gt;
&lt;p&gt;The practical takeaway: Bedrock model choice is becoming an ongoing engineering practice. Claude Sonnet 5 may be a strong option, but the mature response is measured evaluation and targeted adoption.&lt;/p&gt;</content:encoded></item><item><title>Bedrock managed entitlements make model access a platform control</title><link>https://theawsblog.com/news/emiliano-montesdeoca/bedrock-managed-entitlements/</link><pubDate>Tue, 30 Jun 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://theawsblog.com/news/emiliano-montesdeoca/bedrock-managed-entitlements/</guid><description>Amazon Bedrock managed entitlements let organizations subscribe to marketplace models centrally and distribute access across accounts without broad AWS Marketplace permissions.</description><content:encoded>&lt;p&gt;AI model access becomes messy as soon as an organization moves beyond one account and one team. Some models are available directly. Others require AWS Marketplace subscriptions. Workload accounts need access, but broad Marketplace permissions are rarely what security teams want.&lt;/p&gt;
&lt;p&gt;The AWS Machine Learning Blog post on &lt;a href="https://aws.amazon.com/blogs/machine-learning/simplify-multi-account-access-to-amazon-bedrock-models-with-managed-entitlements/"&gt;managed entitlements for Amazon Bedrock models&lt;/a&gt; is important because it turns model access into a platform-governance problem instead of an account-by-account chore.&lt;/p&gt;
&lt;h2 id="what-changed"&gt;What changed&lt;/h2&gt;
&lt;p&gt;Managed entitlements let a central account subscribe to supported third-party Bedrock models distributed through AWS Marketplace and share access with member accounts using AWS License Manager. Workload accounts can use the model access without needing direct AWS Marketplace subscription permissions.&lt;/p&gt;
&lt;p&gt;This is especially useful for models such as Anthropic, Cohere, AI21 Labs, or Stability AI when they are distributed through Marketplace and used across many accounts.&lt;/p&gt;
&lt;h2 id="why-builders-should-care"&gt;Why builders should care&lt;/h2&gt;
&lt;p&gt;A healthy multi-account AI platform needs two things at the same time:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;teams can access approved models quickly,&lt;/li&gt;
&lt;li&gt;the organization can govern subscriptions, pricing, visibility, and permissions centrally.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Without a central entitlement pattern, every workload account becomes a small procurement and governance island. That slows adoption and creates inconsistency. With managed entitlements, a platform team can subscribe once, distribute access intentionally, and keep workload accounts away from broad Marketplace permissions.&lt;/p&gt;
&lt;p&gt;This also helps with private offers. If pricing and terms are negotiated centrally, model access should follow that central agreement rather than being recreated account by account.&lt;/p&gt;
&lt;h2 id="the-trade-offs"&gt;The trade-offs&lt;/h2&gt;
&lt;p&gt;Managed entitlements are not needed for every Bedrock model. Amazon models and some partner models may already be available without Marketplace subscription overhead. Single-account teams may not need this complexity.&lt;/p&gt;
&lt;p&gt;For larger organizations, the main design work is governance:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Who approves model subscriptions?&lt;/li&gt;
&lt;li&gt;Which accounts receive grants?&lt;/li&gt;
&lt;li&gt;How are Regions handled?&lt;/li&gt;
&lt;li&gt;How are private offers tracked?&lt;/li&gt;
&lt;li&gt;How is model use monitored against budget and policy?&lt;/li&gt;
&lt;li&gt;What is the offboarding process when a model is no longer approved?&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Access distribution is only one layer. Teams still need IAM permissions, guardrails, logging, evaluation, and cost controls around actual model invocation.&lt;/p&gt;
&lt;h2 id="what-to-do-next"&gt;What to do next&lt;/h2&gt;
&lt;p&gt;Inventory current Bedrock model usage by account. Identify which models require Marketplace subscriptions and which accounts have Marketplace permissions only because they needed model access.&lt;/p&gt;
&lt;p&gt;Then pilot managed entitlements with one approved third-party model and a small set of workload accounts. Validate the subscription flow, grant distribution, regional behavior, billing visibility, and access removal.&lt;/p&gt;
&lt;p&gt;The practical takeaway is that AI platforms need the same governance maturity as any other shared platform capability. Managed entitlements give AWS organizations a cleaner control point for model access.&lt;/p&gt;</content:encoded></item></channel></rss>